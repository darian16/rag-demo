import React from 'react';
import LogoSection from "../LogoSection";
// ==============================|| MAIN NAVBAR / HEADER ||============================== //

const Header = () => {
  return (
    <React.Fragment>
      <LogoSection />
    </React.Fragment>
  );
};

export default Header;
